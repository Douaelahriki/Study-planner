import { NotificationListComponent } from './notification-list';

describe('NotificationListComponent', () => {
  it('should create', () => {
    expect(NotificationListComponent).toBeTruthy();
  });
});